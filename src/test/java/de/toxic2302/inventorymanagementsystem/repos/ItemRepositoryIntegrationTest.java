package de.toxic2302.inventorymanagementsystem.repos;

import static org.assertj.core.api.Assertions.assertThat;

//@RunWith(SpringRunner.class)
//@DataJpaTest
public class ItemRepositoryIntegrationTest {

    /*@Autowired
    private TestEntityManager entityManager;

    @Autowired
    private ItemRepository repository;

    @Test
    public void whenFindByName_thenReturnItem() {
        // given
        final Item item = new Item("item");
        entityManager.persist(item);
        entityManager.flush();

        // when
        final Item found = repository.findByName(item.getName());

        // then
        assertThat(found.getName()).isEqualTo(item.getName());
    }*/
}
